# CHANGELOG

版本號規則：[語意化版本](https://semver.org/lang/zh-TW/)（MAJOR.MINOR.PATCH）。
- MAJOR：不相容的重大改版（目前還沒發生過，1.x 都還在同一個大版本內）
- MINOR：新功能、使用體驗改版（例如四步流程、新評分模式）
- PATCH：修 bug、小調整，不影響既有操作方式

`worker/package.json`、`web/package.json`、`worker/wrangler.jsonc` 的 `APP_VERSION`
三處版本號一起手動更新（沒有自動化，改版時記得三個都要動）。`使用說明書.md`／`.docx`
的版本紀錄表格也要同步更新。線上 `/health` 會回報目前版本，發布後可以直接核對。

## v1.17.0（2026-09-25）
**不用再守著畫面等 AI：學生交了作業，classAI 會自己去 Classroom 拿來先評好，你打開首頁就看到「等你確認」。**

- **背景自動預批**：一份作業只要設好評分標準（或打開過批改頁），接下來 21 天 classAI 每 10 分鐘
  自己去 Classroom 看有沒有新繳交，學生交出後 10 分鐘沒再修改就先評好（學生連續重交不會重複花 AI 次數）
- **首頁「等你確認」**：列出已經評好、等你看的作業，每份顯示🟢🟡🔴各幾位、幾位要你自己批、幾位學生重交，
  點一下直接進批改頁
- **「要你自己批」**：只交連結（YouTube、Canva…）、檔案讀不到、被 AI 安全機制擋下、AI 連續 3 次沒評成功的，
  不會亂給分，標成「要你自己批」並寫出原因；學生重交後會自動再讓 AI 試一次
- **不會蓋掉你的判斷**：學生重交時，AI 建議你還沒動過的會自動重評；你改過或確認過的分數一律不動，
  列在「學生重交」提醒你自己決定
- 背景評分一樣算進你每天的 AI 次數；同一門課有多位老師時，由最近打開批改頁的那位老師的帳號去 Classroom 拿資料、扣次數
- Google 授權過期時，首頁會提醒重新登入，重新登入並打開批改頁後就恢復
- 開發：`npm run test:it` 修好（v1.15 加 MCP 後一直跑不起來，原因是測試載入不了 `cloudflare:workers`，不是 Node 版本），
  新增背景預批 15 項測試，共 127 項全過

## v1.16.0（2026-09-23）
**評分標準可以直接沿用你在 Google Classroom 設定過的評分量表，AI 評分結果也多了「這份可不可以放心採用」的提示。**

- **讀取 Classroom 評分量表**：如果這份作業你已經在 Classroom 網頁設定過評分量表，進 classAI
  設定評分標準時會看到提示，一鍵套用就好，不用在這裡重打一次
- **三色分流**：AI 評分完，畫面上會看到「🟢可直接確認／🟡建議看一下／🔴需要確認」的統計，
  每位學生的建議旁邊也有對應顏色標示，幫你抓出真正需要仔細看的幾份，不用每份都重新檢查一次
- **複製全班分數**：評分頁新增「複製全班分數」按鈕，一鍵把姓名和分數複製到剪貼簿，
  貼到 Classroom 成績簿比較快，不用另外開 Excel

## v1.15.0（2026-09-22）
**新增 MCP 支援：老師可以在 Claude Desktop 這類支援 MCP 的 AI 助理裡，直接用自然語言查詢自己的
Classroom 資料，不用開瀏覽器登入 classAI 網頁。**

這次先開放 5 個唯讀查詢工具：
- 列出自己的課程／作業
- 找出低分學生（跨作業條件查詢，Classroom 網頁本身做不到）
- 查單一學生的跨作業成績趨勢
- 用關鍵字搜尋自己寫過的評語
- 健康檢查

用法：老師在支援 MCP 的 AI 助理裡連到 `https://classai.hunglun.com/mcp`，第一次連線會跳出
Google 登入視窗確認身分（只需要登入過classAI網頁的帳號，不會重新要求 Classroom 權限），之後
就能直接用聊天方式問「這學期誰進步最多」這類跨作業的問題。目前只能查詢，不能透過 MCP 改分數或
寫入資料。

## v1.14.1（2026-09-22）
**AI 評分改用 9 把 API key 輪流呼叫，比較不會遇到「今天額度用完」。**

老師端沒有任何變化，純粹是後台可靠度改善：以前 AI 評分只用一把 Gemini key，用量大的時候
容易撞到單一 key 的每日額度上限。現在改成 9 把 key 輪流使用，每次評分依序換下一把，額度
分散在 9 把上，同一把被打到限制時也會自動借用下一把頂上完成這次評分。

## v1.14.0（2026-09-22）
**AI 評分會記住你改過的分數，同一份評分標準用越多次，AI 拿捏越準。**

以前老師改完分數、按確認定案後，那次修正就結束了，AI 下次評同一份評分標準還是從頭猜。現在：
- 老師確認分數時，如果 AI 建議分跟老師最終定案分差距超過總分 15%（算是真的改過，不是微調），
  就把「學生作答節錄、AI 原本建議分、老師改成幾分、老師評語」存成這份評分標準的校準範例
- 之後同一份評分標準再評分時，會帶最近 3 筆校準範例給 AI 參考，幫助 AI 拿捏這位老師的鬆緊標準
- 每份評分標準最多留 8 筆範例，超過會自動汰換最舊的一筆，不會無限累積
- 這些範例只在同一份評分標準（同一位老師）內使用，不會被其他老師看到或用到

## v1.13.0（2026-09-21）
**標準答案檔可以直接傳 Word 和純文字檔。**

以前答案檔只收照片、PDF、Excel，老師手上是 Word 還得先另存 PDF。現在多收：
- Word（.docx）：上傳時解析成文字，含表格
- 純文字（.txt、.md、.csv）
- 舊版 .doc 讀不了，會提示另存成 .docx 或 PDF
- 檔案裡讀不到任何文字（例如整份是圖片）會直接說明，不會存成空的答案

**順手修好一個 Word 解析 bug**：Word 表格或段落裡純數字的文字（例如題號 1 到 10、學生只寫「10」的格子）
會整個消失，因為 XML 解析器把數字轉成 number 而程式只認字串；前後空白也會被修掉，
造成「Hello 」＋「world」黏成一個字。這會影響學生交的 Word 作業，現在都原樣保留。

## v1.12.0（2026-09-21）
**老師打開批改頁，看到的是已經評好的結果，不用一位一位按。**

以前拉完學生繳交後，老師要自己按「讓 AI 評分還沒評的 N 位」。現在進頁面或按「更新學生繳交」後，
已繳交但還沒評的學生會自動批次評分（同時 3 位，評好一位出現一位）。
- 每位學生只自動評一次，失敗的留給「只重評失敗的」，不會無限重試
- 只評到今天剩餘額度為止，不彈確認框；超過的部分還是可以自己打分
- 老師改過或確認過的學生（已有狀態）不會被自動評分碰到

## v1.11.3（2026-09-20）
**登入被擋住的真正原因，實測後修好。**

Steve 第一次實機登入一直卡在「有權限沒有勾到」，他其實每一項都勾了。接上 Worker 即時 log 才看到：
我們跟 Google 要 `classroom.coursework.students.readonly`，Google 一律換成範圍較小的
`classroom.student-submissions.students.readonly` 發回來。同一個帳號連試兩次，回來的六個權限一模一樣，
所以不是老師漏勾。這個 App 還沒送 Google 正式驗證，推測與此有關。

改法：權限檢查從「每一個都要有」改成「分組，每組有一個就算數」，
`coursework.students.readonly` 與 `student-submissions.students.readonly` 視為同一組。
真的權限不夠用的話，會在讀作業清單時回 403，那裡本來就有看得懂的錯誤訊息。

## v1.11.2（2026-09-20）
登入相關的兩個防呆，都是 Steve 第一次實機登入時撞到的：

- **登入導向不再被瀏覽器留在快取裡**：`/api/auth/google/login` 的跳轉加 `Cache-Control: no-store`。
  之前 Google Cloud Console 的導回網址改過之後，老師的分頁若重送舊的那一條，會看到
  `redirect_uri_mismatch`（畫面只說「這個應用程式的要求無效」），很容易誤以為是自己的帳號有問題。
- **權限沒勾齊時，直接點名少了哪一項**：以前只說「有權限沒有勾到」，老師得自己一項一項對。
  現在後端把少掉的權限帶回登入頁（`?missing=courses,drive`），畫面顯示「這次少了：查看你的
  Google Classroom 課程、查看你 Google 雲端硬碟裡的檔案」，後端 log 也會記下實際拿到的權限字串，
  方便判斷是老師沒勾還是 Google 沒給。

## v1.11.1（2026-09-20）
修正：Pages 的 `/api` 轉送層會覆寫請求的 `Origin`，導致 v1.10.0 加的來源檢查（防 CSRF）在正式環境失效。
上線後打線上端點驗證時抓到（別的網站冒用身分預期 403，實際只回 401＝只是沒登入而已）。
轉送層不再動這個欄位，瀏覽器送什麼就照傳。

## v1.11.0（2026-09-20）
每位老師的 AI 用量上限，一位老師用過頭不會影響其他人：

- 以前完全沒有限制，所有老師共用同一把 Gemini 金鑰，一位老師連點就可能把當天額度吃光，其他老師全部評不了
- 每人每天 200 次、每分鐘 20 次（`wrangler.jsonc` 的 `DAILY_AI_LIMIT`／`MINUTE_AI_LIMIT`，要調不用改程式）
- 每天的用量記在新表 `ai_usage`（migration 0012），按台灣時間換日；每分鐘的計數放 KV
- **只有真的打了 AI 並成功才算一次**：鎖定、沒繳交、檔案讀不到這些提早結束的不算，AI 自己失敗（額度、逾時）也不算
- 額度用完：AI 評分回 429 並說明「明天會重置，還是可以自己打分」，**手動打分完全不受影響**
- 批改頁顯示「今天還可以讓 AI 評 N 份」；用完時批次按鈕停用並說明原因；
  要評的人數超過剩餘次數時先提醒會評到第幾位
- 新增 `GET /api/usage`（今天用了幾次、上限、還剩幾次），之後按學校收費也有紀錄可查
- 整合測試新增 10 項（共 99 項）

## v1.10.0（2026-09-20）
前後端改成同一個網域，修好 iPad／Safari 登不進去：

- 以前前端在 classai.hunglun.com、API 在 workers.dev，是兩個網域，登入狀態屬於「第三方 cookie」，
  **Safari（iPad／iPhone／Mac）預設整個擋掉**，老師會一直被踢回登入頁（Steve 遇過的「無痕視窗登不進去」同源）
- 新增 `web/functions/api/[[path]].js`：Pages 把 `classai.hunglun.com/api/*` 轉給後端 Worker，
  瀏覽器只看到一個網域，cookie 變成第一方。不動 DNS、不靠「Worker 路由蓋過 Pages」這種沒寫在文件裡的行為
- 內測密碼閘跳過 `/api/*`（不跳過的話 Google 登入導回會被密碼閘擋掉）
- 登入 cookie 從 `SameSite=None` 改回 `SameSite=Lax`＋Secure
- 新增來源檢查：會改資料的請求（POST/PATCH/DELETE）如果不是從 classAI 自己的網站送出，一律擋下（防 CSRF）
- Google 登入導回網址改成 `https://classai.hunglun.com/api/auth/google/callback`
- 整合測試新增 5 項（共 89 項）
- **發布時要另外做三件事**：Pages 環境變數 `VITE_API_BASE` 改成 `https://classai.hunglun.com`、
  新增 `WORKER_ORIGIN`；Google Cloud Console 的 OAuth 用戶端加上新的導回網址

## v1.9.0（2026-09-20）
協同教學的課程，兩位以上老師都能用：

- 以前一門課只記一位老師（誰先同步記誰），第二位老師點進去每一步都是「找不到這門課，或不屬於你」，等於不能用
- 新增 `course_teachers` 表（migration 0011），每位老師同步課程時把自己加進去；能不能用改看這張表。
  Classroom 是用「我教的課」去查的，查得到就代表是這門課的老師，等於 Google 幫忙驗證過身分
- 同一門課的老師共用同一份成績：誰改的其他人都看得到，也都能確認、解鎖、看同一份修改歷程、下載成績表
- 不是這門課的老師照樣什麼都看不到（10 支 API 全部擋下）
- 老師自己存的評分範本仍然是各自的，不共用
- 課程清單顯示「N 位老師共用」，批改頁提醒分數和評語是大家共用的
- 既有課程由 migration 自動搬進新表，原本的老師不受影響
- 整合測試新增 12 項（共 84 項）

## v1.8.0（2026-09-20）
全 App 防呆總檢查（Steve：「要把老師當成笨蛋，把 APP 做到所有防呆機制」）。原則：擋得下來就擋、
擋不下來就講清楚、做錯了救得回來，錯誤訊息一律白話並附下一步該按什麼。

- **登入**：在 Google 按取消、權限沒勾滿、登入頁停太久、沒拿到完整授權、其他失敗，一律回登入頁顯示白話說明；
  權限沒勾滿（例如沒勾雲端硬碟）在登入當下就擋，不再等到之後功能莫名壞掉；登入按鈕按一次就鎖住
- **登入過期**：任何一頁發現登入過期（14 天）或 Google 授權過期，整個 App 回到登入頁並說明原因
- **網路斷線**：不再顯示英文 Failed to fetch，改「連不上伺服器，請檢查網路後再試一次」；載入失敗都有「再試一次」
- **Classroom 拒絕／找不到**：講出最可能的原因（學校管理員沒開放、作業被刪除或封存）和該怎麼做
- **還沒存的修改**：評分標準頁、批改卡片有沒存的修改，或 AI 正在批次評分時，按上一步、logo、登出、關分頁都會先問
- **選擇題**：Classroom「選擇題」題型的作答以前沒讀到（全班會被當成空白繳交），已補上
- **評分標準頁**：總分空白／0／負數說明原因；總分跟 Classroom 不同時提醒；已有學生評過分時提醒改了不會自動重評，
  總分改得比已給的分數低時紅字警告；量表配分 0／負數、名稱重複指出第幾項；套用範本會蓋掉內容時先問；
  上傳 Word、iPhone HEIC、其他不支援的檔案各有說明，改看副檔名判斷；範本名稱上限提醒
- **學生重交提醒**：同步時記下最後一次繳交時間（migration 0010），評分後又重交的學生在卡片、收合列顯示提醒，
  並多一個「學生重交」篩選
- **後端補驗證**：評分標準總分、配分、加總、名稱重複、文字長度；Excel 解析失敗不再回傳技術錯誤
- 整合測試新增 17 項（共 72 項）

## v1.7.0（2026-09-20）
AI 失敗時老師可以自己打分、可以打開學生原檔，並補上防呆：

- **自己打分**：沒評過分的學生多一顆「自己打分」（AI 評分失敗時也有），填分數評語按「完成批改」就算數。
  後端 `PATCH .../grade` 改成沒有評分紀錄就新增（原本只能改 AI 評過的，沒有就回 404）
- **打開學生原檔**：卡片列出學生交的每個檔案／連結，點了開新分頁。同步繳交時存下雲端硬碟原檔連結，
  舊資料用檔案 ID 組連結。只允許 http／https，`javascript:` 之類的連結不做成可點的（防 XSS）
- **分數防呆**：分數欄清空不再默默變成 0 分；沒填、不是數字、超出 0～總分都擋下來，游標回到分數欄；
  後端也檢查分數範圍與評語上限（5000 字），超過回 400 並說明原因
- **AI 重評不會默默蓋掉老師的修改**：老師改過的（或還有沒存的字），第一次按只提醒、第二次按才重評；
  後端沒收到確認（`?force=1`）也會擋下
- **手動打分後清掉「評分失敗」標記**，避免「只重評失敗的」把老師的分數蓋掉
- **還沒存的修改**：關分頁／重新整理前瀏覽器會先提醒
- 整合測試新增 16 項（共 55 項）

## v1.6.2（2026-09-19）
擋住「學生在作答裡對 AI 下指令、騙到滿分」（提示詞注入）：

- 老師的評分規則改放 Gemini 的系統指示；學生作答與附件包在每次隨機產生的邊界標籤裡，只當資料不當指令，
  學生打 `"""` 之類的符號也跳不出去
- AI 回傳固定格式（responseSchema），多回報一欄「有沒有人在對 AI 下指令」
- 後端另外比對常見句型（給滿分、忽略以上指示、系統通知、老師已審核、ignore previous…），不依賴 AI 判斷
- 任一偵測成立，AI 建議上方出現警示「作答內容疑似在對 AI 下指令，這個分數請自己看過作業再確認」
- Gemini 金鑰改放請求標頭，不放在網址裡
- 實測（真的打 Gemini）：改版前 8 種攻擊有 4 種被騙成 10/10、0 次警示；改版後 flash-lite 與 flash
  各 8 種攻擊全部 ≤ 2 分且都有警示，正常作答（含「不要忽略」字樣的作文、量表模式）分數正確、沒有誤判
- 新增 `npm run test:injection`（真的打 Gemini，會用額度）與 `worker/test/fixtures/`

## v1.6.1（2026-09-19）
修正大檔案會出錯的問題：

- 標準答案檔（圖片／PDF）改存 Cloudflare R2，資料庫只記檔案位置。原本存在 D1，
  D1 每一列上限 2MB（任何方案都一樣），原始檔超過約 1.4MB 就存不進去；上傳上限維持 8MB。
  已經存在 D1 的舊答案檔照樣讀得到，不用搬
- 學生附件加大小上限：單檔 15MB，一次評分所有檔案（含答案檔）合計 20MB，避免 Worker
  記憶體（128MB）不夠。超過的檔案不下載，AI 建議上方會出現「有 N 個附件太大 AI 沒讀到」；
  全部都太大就直接請老師開原檔
- Word（.docx）附件只解壓內文，裡面的圖片不再佔記憶體；內文解壓後超過 20MB 的異常檔案直接擋下
- AI 沒有任何作答內容可看時不再照樣給分：空白繳交、只交連結、只交試算表／表單／影片、檔案讀不到或太大，
  一律不叫 AI，直接告訴老師原因；有文字但部分附件讀不到時，照評並加警示「有 N 個附件 AI 讀不到」
- 送出格式錯誤的資料改回 400「資料格式不對」，原本回 500「系統暫時發生問題」
- 手機版：警示框與錯誤框裡的長網址／長檔名會自動換行，不再把整頁撐出橫向捲軸（360 寬原本撐到 641px）
- 每一頁最下方固定顯示「classAI v版本號」（含登入頁），版本號打包時從 `web/package.json` 讀，不在別處寫死
- 新增整合測試 `worker/test/answer-key-and-attachments.it.mts`（`npm run test:it`，39 項＋真的 Gemini 2 項）
- 新增 migration `0009_rubric_answer_key_r2.sql`、R2 bucket `class-ai-answer-keys`

## v1.6.0（2026-09-19）
十輪功能討論MVP範圍最後一項：簡版可信度提示。AI評分後端算兩條heuristic規則
（不用Gemini自評信心，ChatGPT討論時否決了模型自評信心的可靠度），提醒老師哪些
AI建議值最該仔細看：

- 量表模式（rubric）AI沒有針對全部評分項目給分數
- AI給了0分或滿分

批改頁AI建議值上方會出現紅色警示框列出原因，不影響分數本身（仍只是建議值，
老師照樣可以直接改）。`grades`表新增`confidence_flags`欄位（JSON字串陣列，
沒有警示就是NULL）。

## v1.5.0（2026-09-19）
十輪功能討論MVP範圍剩下的3項全部做完：

- **Rubric線性換算：發現本來就已經做過了**。09-17做範本功能時寫的`splitPoints()`
  （依權重比例分配到目標總分，四捨五入差額補最後一項）就是討論裡要的LINEAR換算，
  這次沒有新增重複的邏輯，套用個人範本時直接複用同一支函式
- **評分標準範本庫**（文字only，不含答案檔附件——刻意避開十輪討論抓到的架構地雷：
  base64答案檔存進範本會讓資料庫暴增）：新表`rubric_templates`；
  `GET/POST /api/rubric-templates`、`GET/DELETE /api/rubric-templates/:id`；
  設定頁加「⭐存為我的範本」與「我的範本」清單，套用時rubric項目照比例換算到目前作業
  的總分（沿用`splitPoints`）
- **docx純文字解析**：新增`worker/src/lib/docx.ts`，用`fflate`解壓+`fast-xml-parser`
  解析`word/document.xml`（不是regex——Word常把一個詞拆成多個`<w:t>`片段，正確處理
  要合併同段落內容；表格結構保留成`|`分隔），接進`drive.ts`讓學生上傳的.docx附件現在
  讀得到內容
- 已驗證：兩邊`tsc`過；docx解析用python-docx產生含粗體拆run與表格的測試檔，實際跑過
  確認正確還原（不是只看程式碼會不會編譯）；範本庫API本機`wrangler dev`全部端點測過
  （建立/列表/取詳細/刪除）；`gemini browse`實機截圖確認「存為我的範本」在真實瀏覽器
  （非curl，避開中文編碼問題）中文顯示與React state更新都正確

## v1.4.0（2026-09-19）
十輪geminiA×ChatGPT討論收斂出的P0：老師確認過的評分不能被「請AI重評」悄悄蓋掉，且要能
回顧「為什麼分數變了」。討論原本設計是三張表（student_submissions/submission_versions/
score_history）的完整多版本快照系統，實作時改用較輕量但保證同樣安全性的設計：一個鎖定
旗標＋一張修改歷程表，範圍收斂到真正15天MVP裡最核心的那塊，沒有照抄討論裡的完整schema。

- `grades`表加`locked`欄位，老師按「完成批改」自動鎖定；鎖定後「請AI重評」與直接改分數/
  評語都會被擋（回409），要先按「解鎖重新編輯」
- 新表`grade_history`：AI初評／AI重評／老師編輯／老師確認／老師解鎖每次異動都留一筆
  （來源＋分數＋評語＋時間），老師可在卡片上展開「看修改歷程」回顧
- 新路由`POST /api/submissions/:id/unlock`、`GET /api/submissions/:id/history`
- 前端：鎖定的卡片分數/評語欄位顯示唯讀，動作列從「完成批改/先存起來/請AI重評」換成
  「解鎖重新編輯」
- 已驗證：本機`wrangler dev`直接打API測完整流程（確認→自動鎖定→AI重評被擋(409)→直接
  改分被擋(409)→解鎖→可再編輯→歷程正確記錄3筆），`gemini browse`（獨立瀏覽器）截圖
  確認前端渲染正確（鎖定卡片按鈕正確替換、歷程清單正確顯示）

## v1.3.0（2026-09-19）
Steve要求跟geminiA／ChatGPT三方接力討論classAI怎麼商業化（5輪），收斂出的第一個可以馬上動手
做的技術結論：新增「AI評分校正紀錄」，衡量老師實際修改AI分數的比例（商業化第一步要驗證的
核心指標：AI評分要低於15~20%修改率才算可信）。

- 新表`grading_calibration_logs`（migration 0005）：只存mode／總分／AI分數／老師最終分數，
  **不存學生姓名、作業原文、老師身分**——三方討論時geminiA原本提案用正規表達式清洗學生作文
  文字再存檔宣稱「符合個資法」，被ChatGPT點出regex抓不到藏在語意裡的個資（例如「我叫王小明」
  沒有「姓名：」前綴），這個宣稱過度樂觀，最後改成根本不存原文，只存分數，從根源避開風險
- `PATCH /api/submissions/:id/grade`：老師按「完成批改」（confirm=true）時順手記一筆，記錄
  失敗不影響這次確認（try/catch包住，不是關鍵路徑）
- 新增`GET /api/calibration/summary`：回傳全站聚合的修改率／平均分差百分比／依評分模式分類，
  任何登入老師都能看（資料本身已完全匿名，沒有可回溯的個人資訊）
- 本機`wrangler dev`實測：AI 82分老師改90分（分差8%，未達15%門檻不算「真的改過」）／AI 95分
  改60分（分差35%，算改過）→ summary正確算出50%修改率；未登入打summary正確擋401

## v1.2.3（2026-09-18）
GCP OAuth同意畫面應用程式名稱改為classAI（Console設定，非程式碼，多輪累積未做的最後一項）。
Steve要求「提升UI/UX」，實機起本機worker+web dev server、灌測試假資料、用`gemini browse`
走查各頁桌機/手機畫面，抓到並修掉：

- **「上傳檔案評分」模式標籤退回舊名**：09-15曾明確要求把「標準答案比對」模式改名成
  「上傳檔案評分」（Steve原話「講清楚一點」），但09-17那輪整頁重寫`RubricSetup.tsx`時
  沒帶上這個決定，畫面又變回「對照標準答案」——是個規格回歸，不是新設計。改回
  「上傳檔案評分」（`MODE_OPTIONS`／`MODE_LABEL`）
- **已完成收合卡片沒有位置編號**：批改頁每張卡片右上角有「1/5」這種位置提示方便對照，
  但學生批改完收合成一行後這個提示消失，跟其他卡片不一致。收合列補上同樣的位置編號

## v1.2.2（2026-09-17）
Steve要求「深度檢查UI/UX以及BUG」，讀完所有前後端程式碼＋實機測試抓到三個真的會影響
使用的問題，全部修掉：

- **未繳交學生的畫面完全是假的**：前端從沒檢查`submission.state`（Classroom回傳的「有沒有
  交作業」），沒交作業的學生卡片上寫「學生交的是檔案，AI評分時會一起讀」——這是假的，他根本
  沒交。連帶「讓AI評分還沒評的N位」的N會把沒交的人也算進去（點下去保證失敗，還被標成「評分
  失敗」的紅卡，但這不是評分失敗是沒交作業），「全班都批改完了」的判斷條件也要求連沒交的人
  都要確認，只要班上有一位缺交這個提示永遠不會出現。三處一起修：新增「還沒繳交」徽章與提示、
  批次評分/進度分母都排除未繳交的人
- **Google授權過期只會看到「系統暫時發生問題」**：這個是實測時真的撞到的（本機那組refresh
  token剛好過期），不是假設情境——Google規定測試中未驗證App的refresh_token約7天到期，
  acerking1、slash這些老師用一週後一定會遇到。原本任何路由呼叫Google API失敗都被全域錯誤
  處理攔截成通用訊息，看不出要重新登入才能解決。新增`GoogleAuthExpiredError`專門類別，
  401回具體訊息＋`code:"auth_expired"`，前端`Courses.tsx`偵測到這個code直接給「重新登入」
  按鈕
- **作業截止時間不保證是台灣時間**：`formatDue`用`getHours()`/`getDate()`這類本地時間函式，
  顯示的其實是「瀏覽器系統時區」，只有系統剛好設台灣時區才會恰好對。改成手動平移UTC+8小時
  再用`getUTC*`讀，不管老師的電腦/手機系統時區設在哪裡，顯示的都是正確台灣時間

## v1.2.1（2026-09-17）
- 修正錯誤文案：「用個人 Gmail 登入會找不到課程」是錯的，Google Classroom 個人帳號也能開課，
  真正的判斷條件是「有沒有登入開課的那個帳號」。改了登入按鈕文字、提示文字、找不到課程的
  空狀態說明（`Login.tsx`、`Courses.tsx`），`使用說明書.md`／`guide/index.html` 同步修正，
  guide 頁的登入截圖也重拍

## v1.2.0（2026-09-17）
- 新增公開的圖文使用手冊 `classai.hunglun.com/guide`：真實畫面截圖＋完整四步流程說明＋
  常見問題，密碼閘一視同仁保護（跟主站同一組密碼），登入頁加「詳細使用說明」連結
- 純靜態頁面（`web/public/guide/index.html`），不吃 Worker API，跟主站分開維護

## v1.1.2（2026-09-17）
- 批次評分不再整班重拉：`gradeOne` 每評完一位就整班重新 GET 的疏漏已修掉，改成直接合併
  後端回傳的結果進本地畫面，30 人的班批次評分不再疊加 30 次多餘請求
- 評分（`ai-grade`）、改分（`PATCH grade`）兩條熱路徑各省一次 D1 來回，把權限檢查併進
  資料查詢／更新裡
- 拿掉 rubrics 查詢過時的 `ORDER BY created_at DESC LIMIT 1`（09-15 已改真正 upsert）
- 綁定自訂網域 `classai.hunglun.com`，`APP_URL` 跟著換（舊網址 `class-ai.pages.dev` 的
  API 呼叫會被 CORS 擋掉，等於停用）
- 補上 GitHub Actions CI，Worker 從此 push main 就自動部署，不用再手動 `cf deploy`

## v1.1.1（2026-09-17）
- 下載成績表（Excel）從 `<a href>` 直連 Worker 網域，改成跟其他 API 同一條
  `fetch(..., {credentials:"include"})` 取 Blob 再存檔，修掉正式環境可能因跨網域
  cookie 下載失敗的疑慮（原本沒人驗證過這條路徑帶不帶得到登入 cookie）
- Worker CORS 加 `exposeHeaders: ["Content-Disposition"]`，前端才讀得到後端算好的中文檔名
- 補上版本管理：三處版本號統一、`/health` 回報版本、建立這份 CHANGELOG
- `使用說明書.docx` 修正一個既有排版瑕疵：「把成績登記進 Classroom」那組列表編號
  接續了另一組列表變成「3. 4.」而非「1. 2.」（Word 的 `List Number` 樣式預設共用同一個
  編號序列，沒有明確指定 numId 就會全文件連續編號）；順便把 09-02～09-15 遺失的版本
  歷史記錄補回版本紀錄表格

## v1.1.0（2026-09-17）
使用體驗大改版（跟 Gemini 討論十輪後動手）：
- 流程改成四步（選課程→選作業→設定評分標準→批改學生），設定評分標準獨立成一頁
- 新增六種評分範本（作文、閱讀心得、學習單、數學計算題、英文寫作、專題報告）
- 評分標準配分加總即時檢查（不等於總分會標紅且不能存）
- AI 失敗訊息改白話（分類原因：額度用盡／學生未繳交／附件讀不到等）
- 「全部 AI 評分」改成只評還沒評過的人，不會蓋掉老師已確認的分數；批次評分改併發
- 批改頁重寫：篩選標籤、進度條、只重評失敗的、已完成收合、複製分數與評語、
  上一位/下一位、Ctrl+Enter 完成並跳下一位、下載 Excel 成績表
- 課程/作業列表加搜尋、依截止日排序、空狀態換帳號引導
- AI 評語固定三段格式：【做得好】【可以更好】【下一步】
- 新配色、字級放大、三張插圖（登入頁／空狀態／全部完成）

## v1.0.4（2026-09-15）
- 「標準答案比對」模式改名「上傳檔案評分」，支援上傳圖片／PDF／Excel 答案檔（8MB 內）
- Excel 答案檔用 `@e965/xlsx` 解析成文字表格再餵給 AI（SheetJS 官方版有未修復 CVE，改用維護中 fork）
- rubrics 資料表改成真正 upsert（`ON CONFLICT DO UPDATE`），不再每次存都長出新版本
- 批次評分改併發（`BATCH_CONCURRENCY=3`）、Gemini API 呼叫加 30 秒 timeout
- 鴻綸科技標誌放大改清楚版本，Header／Footer／登入頁都補上

## v1.0.3（2026-09-14）
- 修好正式環境登入無限循環：session cookie 在正式環境改成 `SameSite=None; Secure`
  （前端 pages.dev 跟後端 workers.dev 是跨網域，`SameSite=Lax` 的 cookie 帶不過去）
- 修好登入後老師姓名亂碼：`decodeIdToken` 的 `atob()` 沒處理多位元組 UTF-8，改用
  `TextDecoder` 正確解碼
- 產品名稱改成「classAI」；AI 評分優先順序改成 flash-lite 優先（pro 額度常打光）
- 評分頁改版：作業標題橫幅、評分標準預設指令、自動帶入 Classroom 原始滿分、
  評分模式改卡片式選單；加上步驟指示條、麵包屑、首次使用說明卡片
- 內測密碼閘從瀏覽器原生 HTTP Basic Auth 改成自訂 HTML 密碼頁（cookie-based）

## v1.0.2（2026-09-14）
- 使用說明書補上正式網址、進站密碼、進階說明、疑難排解章節

## v1.0.1（2026-09-10）
- 修正登入 `invalid_client`：舊 GCP 專案（shen@gfeh.org 名下）的權限邊界問題，
  整套改用 hunglun2026 名下全新 GCP 專案（`class-ai-v2`），重建 OAuth 用戶端與 Gemini 金鑰
- 本機、線上端到端登入實測成功

## v1.0.0（2026-09-06）
- 首版使用說明書（.docx）發布
- 正式環境上線：Worker（`class-ai-worker.hunglun2026.workers.dev`）＋
  Pages（`class-ai.pages.dev`）＋內測密碼閘

## v0.2.0（2026-09-03）
- GCP 專案帳號轉移到 hunglun2026@gmail.com

## v0.1.0（2026-09-02）
- GrillMe 需求拷問定案（12+ 題問答），骨架開發完成：Worker（Hono + D1 + KV）
  ＋ React/Vite 前端、Google OAuth、Classroom/Drive API 串接、Gemini 雙模型容錯評分
